{
  "skill_name": "using-hf-workflow",
  "evals": [
    {
      "id": 1,
      "prompt": "User request:\n/hf-review code TASK-003\n\nProject snapshot:\n- This is a review-only request.\n- `Workflow Profile`: standard.\n- `Current Active Task`: TASK-003.\n- `hf-test-review` is already complete and passed.\n- `hf-code-review` has not yet been run.\n- There is no route/profile conflict and the review target is unambiguous.\n\nDecide the correct HF entry action.",
      "expected_output": "Use the public entry layer to recognize a clear review-only request and direct invoke `hf-code-review` rather than bouncing to router or implementation.",
      "files": [],
      "expectations": [
        "The response selects `hf-code-review` as the next action.",
        "The response treats `/hf-review` as an entry bias rather than as standalone authority.",
        "The response does not route to `hf-workflow-router` when the node is already clear.",
        "The response does not route back to `hf-test-driven-dev` or another upstream node.",
        "The final response uses the compact entry format with numbered `Entry Classification`, `Target Skill`, and `Why` lines.",
        "The final response explicitly marks `Entry Classification` as `direct invoke`."
      ]
    },
    {
      "id": 2,
      "prompt": "User request:\n/hf-closeout TASK-003\n\nProject snapshot:\n- `Workflow Profile`: standard.\n- `Current Active Task`: TASK-003.\n- `hf-regression-gate` has passed.\n- `hf-completion-gate` has passed and explicitly allows finalize.\n- Release notes stub exists and closeout is the only remaining step.\n- There is no evidence conflict.\n\nDecide the correct HF entry action.",
      "expected_output": "Use the public entry layer to direct invoke `hf-finalize` because closeout is clear and all required gate evidence is already present.",
      "files": [],
      "expectations": [
        "The response selects `hf-finalize` as the next action.",
        "The response does not send the request back to `hf-workflow-router` when closeout eligibility is already explicit.",
        "The response does not route to `hf-completion-gate` again after an explicit pass.",
        "The response does not write `using-hf-workflow` as a runtime handoff target.",
        "The final response uses the compact entry format with numbered `Entry Classification`, `Target Skill`, and `Why` lines.",
        "The final response explicitly marks `Entry Classification` as `direct invoke`."
      ]
    },
    {
      "id": 3,
      "prompt": "User request:\n继续这个 workflow\n\nProject snapshot:\n- `task-progress.md` says `Current Stage: hf-test-driven-dev`.\n- A fresh reviewer summary says `conclusion=阻塞`, `reroute_via_router=true`, and the issue is an upstream route/profile conflict.\n- Another note says the acceptance criteria may have changed since the last approved task plan.\n- The correct downstream node is not yet obvious.\n\nDecide the correct HF entry action.",
      "expected_output": "Use the public entry layer to recognize this as runtime recovery with conflicting evidence and hand off to `hf-workflow-router` instead of guessing a leaf node.",
      "files": [],
      "expectations": [
        "The response selects `hf-workflow-router` as the next action.",
        "The response explicitly recognizes route/profile/evidence conflict.",
        "The response does not directly invoke `hf-test-driven-dev`, `hf-increment`, or any review/gate node.",
        "The response preserves the boundary that `using-hf-workflow` is entry-only, not runtime authority.",
        "The final response uses the compact entry format with numbered `Entry Classification`, `Target Skill`, and `Why` lines.",
        "The final response explicitly marks `Entry Classification` as `route-first`."
      ]
    },
    {
      "id": 4,
      "prompt": "User request:\n请直接进入 `hf-code-review` 审一下 TASK-003，我只要 code review，不想再绕路。\n\nProject snapshot:\n- `Workflow Profile`: standard.\n- `Current Active Task`: TASK-003.\n- The implementation just finished.\n- `hf-test-review` has not yet been run.\n- There is no approved evidence that the review chain can legally skip straight to `hf-code-review`.\n- The user explicitly named a leaf skill, but the legal current review node is not confirmed.\n\nDecide the correct HF entry action.",
      "expected_output": "Use the public entry layer to reject the illegal direct invoke and hand off to `hf-workflow-router`, because naming `hf-code-review` does not override unresolved review-order evidence.",
      "files": [],
      "expectations": [
        "The response selects `hf-workflow-router` as the next action.",
        "The response does not directly invoke `hf-code-review` when `hf-test-review` is still missing.",
        "The response explicitly explains that naming a leaf skill does not by itself make direct invoke legal.",
        "The response explicitly identifies the missing or unresolved review prerequisite as the reason to route first.",
        "The final response uses the compact entry format with numbered `Entry Classification`, `Target Skill`, and `Why` lines.",
        "The final response explicitly marks `Entry Classification` as `route-first`."
      ]
    }
  ]
}
